2026-05-18 | grants-ingest planning | opus-4.7 | ~30k | ADR-001 committed; plan for grants_ingest funder-registry slice (irs_990pf + propublica_np) | impl deferred; 3 blocking questions for Chris
2026-05-18 | grants-ingest impl-1 | sonnet-4.6 | ~40k | commits 1-8 (scaffold, object store, RawRecord, CorpusEvent, Funder/Program/HistoricalGrant models + migrations); 15 tests passing | stopped mid-session; commits 9-15 queued
2026-05-18 | grants-ingest impl-2 | sonnet-4.6 | ~35k | commits 9-15 (resolver, materializer, BaseAdapter, ProPublicaNP, IRS990PF, 6 mgmt commands, DMV seed list, README, handoff) | slice 1 complete; S3 backend and cron deferred to slice 2
2026-05-28 | attendance coordinator UI | opus-4.8 | ~90k | dashboard/program/session views + attendance record + add-participant, derived headcount queries, base.html design shell, 16 UI tests (70 non-grants total green), no schema change | LLM parse path, coordinator login entry, donor snapshots deferred (see handoff 2026-05-28); later superseded by the dashboard surface, re-introduced as slice 2
2026-05-29 | step4-donor-surface | sonnet-4.6 | ~60k | slices 0-4: bootstrap_org cmd, ProfileSnapshot + slugs + migration, snapshot service + verification-chain tests, discovery app + public page, coordinator publish flow; 102 tests passing | coordinator shell template + program edit form deferred; branch not yet merged to main
2026-05-30 | coordinator-surface slice 1 | opus-4.8 | ~55k | coordinator shell (base.html) + home launchpad (attendance/launchpad.py, home view, /coordinator/ remount, magic-login repoint, attendance_log stub); 13 slice-1 tests, 333 passing | slice 2 (attendance-capture screen the CTA opens) next; framing held, plan unchanged
2026-05-31 | coordinator-surface slice 1.5 | opus-4.8 | ~95k | director dashboard (command center) replaces the attendance launchpad: mono-classy top-bar shell + Tools mega-menu (Live/Beta links, Soon non-links), verified org stat cards + week/month/all period selector, needs-attention (stale attendance + stale profile), programs grid, cross-tool activity feed; design doc + approved static mockup; 342 tests | slice 2 (attendance capture) still next; logout/search/Forms data deferred (honest empties)
2026-05-31 | fix dashboard merge regression | opus-4.8 | ~25k | restored slice-1.5 dashboard after the d670037 merge clobbered the home wiring (attendance views/urls/queries/forms, base.html shell, root-urls coordinator+discovery mounts); removed step-3 attendance-loop orphans (preserved on da53963 / feat/attendance-coordinator-ui); dev-log reconciled to all four sessions; suite green | slice 2 re-introduces the step-3 session→attendance loop
2026-05-31 | coordinator-surface slice 2 planning | opus-4.8 | ~40k | rewrote the Slice 2 section of docs/plans/2026-05-30-coordinator-surface.md from "attendance-only" to the session-wrap multiplier (tap-first attendance + one-tap activity → AI parent-update draft; hours/payroll dropped); two-user model (facilitator tap-first / coordinator AI-parse batch); grounded schema in real models (Session.scheduled_date+notes, AttendanceRecord append-only save() guard, programs_visible_to confirmed); sub-slices 2a/2b/2c + tests; ≤2-field one-table migration | build deferred — /clear + Sonnet to implement 2a against the plan; §2.13 open items (hours, generic chips, 2c placement) to confirm
2026-05-31 | session-guide planning | opus-4.8 | ~55k | STEP 1 plan + ADR for the coordinator-surface Session Guide (docs/plans/2026-05-31-session-guide.md + docs/decisions/2026-05-31-session-guide-data-model-and-midnight-rule.md): schema deltas (Program pay defaults; ProgramFacilitator through-model w/ migration recipe; Enrollment; Session lifecycle closed_at/FoR/duration/auto_closed; FacilitatorPayPeriod), derived ACTIVE/INACTIVE ghosting rule, Midnight Rule cron-not-queue, TIME_ZONE→Eastern; slices A–D w/ tests + re-run fieldwork test; superseded the AI Slice 2; CLAUDE.md phase updated; branch feat/session-guide | build deferred — /clear + Sonnet to implement Slice A against the plan
2026-05-31 | session-guide Slice A (setup) | opus-4.8 | ~70k | Program pay defaults (default_session_length_minutes, default_facilitator) + facilitators M2M→ProgramFacilitator through-model w/ hourly_rate_cents (handwritten SeparateDatabaseAndState migration adopting core_program_facilitators, no data loss; .add() intact) + attendance.Enrollment per-program roster; coordinator-only program_setup view/form/template (defaults + per-facilitator rate + paste-a-list roster, idempotent + PRG), program-detail link, seed_attendance enrolls; 11 new tests, full suite 353 passing | next: Slice B (in-session session guide + Midnight Rule) — schema for B is Session lifecycle fields (closed_at/FoR/duration/auto_closed) + TIME_ZONE→Eastern
2026-05-31 | session-guide Slice B (in-session guide + Midnight Rule) | opus-4.8 | ~110k | open→attendance (get_or_create today, tap-first roster w/ ghosting Inactive divider + "all here" + "+ Add someone", append-only idempotent commit, present/absent only) + wrap (Session.notes + Facilitator of Record, Save&close stamps closed_at+default duration, idempotent); Session lifecycle schema (closed_at/facilitator_of_record/duration_minutes/auto_closed); TIME_ZONE→America/New_York; enrollment_states+latest_status_by_participant derived queries; close_open_sessions Midnight Rule cron command (idempotent, default duration+facilitator, auto_closed, no fabricated note); global flash messages; 22 new tests, full suite 375 passing | next: Slice C (reporting card: trend + note + last-logged) then Slice D (pay prep)
2026-05-31 | session-guide Slice C (reporting card) | opus-4.8 | ~45k | dashboard per-program card extended w/ Slice-B data: recent trend (last 6 sessions present/absent ticks, open/auto-closed marked), latest wrap note, last-logged date + staff actor (FoR → recorder fallback) + auto-closed tag; ProgramCard.recent/latest_note/last_logged_*/last_auto_closed via _recent_trend+_last_logged (reuse latest_status_by_participant); launchpad PII boundary held (counts + staff names only); 6 new tests incl. render PII probe, full suite 381 passing | next: Slice D (pay prep — sessions×duration×rate → reconcile/approve/paid/CSV)
2026-05-31 | session-guide Slice D (pay prep) | opus-4.8 | ~70k | attendance/pay.py (closed sessions × duration × per-(facilitator,program) rate, integer-cents math no float drift, null-rate/null-FoR flagged not zeroed) + FacilitatorPayPeriod (approve/paid state, ADR D9); coordinator-only /coordinator/pay/ month table w/ nav, one-field reconcile (0=cancellation), approve, mark-paid, CSV export; Tools menu "Monthly payroll prep"→Beta; "stops at the math" boundary; 13 new tests, full suite 394 passing | Session Guide A–D COMPLETE on feat/session-guide (not merged); handoff docs/handoffs/2026-05-31-session-guide-complete.md; next: fieldwork w/ Matthew Ratz, then merge
